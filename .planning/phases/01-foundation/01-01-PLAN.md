---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.js
  - tailwind.config.ts
  - postcss.config.js
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/page.tsx
  - components.json
  - .eslintrc.json
  - .prettierrc
autonomous: true
requirements:
  - REQ-01
user_setup: []
must_haves:
  truths:
    - "Next.js 14 App Router project runs with `npm run dev`"
    - "TypeScript compiles without errors"
    - "Tailwind CSS styles apply (dark theme default)"
    - "shadcn/ui components available for use"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      min_lines: 40
    - path: "tsconfig.json"
      provides: "TypeScript configuration with path aliases"
      contains: '"@/*": ["./src/*"]'
    - path: "tailwind.config.ts"
      provides: "Tailwind config with dark mode class strategy"
      contains: "darkMode: 'class'"
    - path: "components.json"
      provides: "shadcn/ui configuration"
      contains: '"style": "default"'
    - path: "src/app/globals.css"
      provides: "Global styles with Tailwind directives and CSS variables for theming"
      min_lines: 50
    - path: "src/app/layout.tsx"
      provides: "Root layout with font and theme provider"
      min_lines: 30
  key_links:
    - from: "package.json"
      to: "next.config.js"
      via: "dependencies"
      pattern: '"next":'
    - from: "tailwind.config.ts"
      to: "src/app/globals.css"
      via: "@import 'tailwindcss'"
      pattern: "@tailwind"
---

<objective>
Scaffold Next.js 14 project with App Router, TypeScript, Tailwind CSS (dark theme), and shadcn/ui component library. This is the foundation all subsequent phases build upon.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>task 1: Initialize Next.js 14 project with TypeScript, Tailwind, and ESLint</name>
  <files>package.json, tsconfig.json, next.config.js, .eslintrc.json, .prettierrc, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css</files>
  <action>
Create Next.js 14 project using `create-next-app` with TypeScript, Tailwind, ESLint, App Router, and src/ directory. Configure:
- package.json: Add dependencies: next@14, react@18, react-dom@18, typescript@5, tailwindcss@3, postcss@8, autoprefixer@10, eslint@8, prettier@3, @types/node@20, @types/react@18, @types/react-dom@18
- tsconfig.json: Strict mode, path aliases (@/* -> ./src/*), target ES2017, moduleResolution bundler
- next.config.js: Enable experimental.serverActions, images.remotePatterns for GitHub avatars
- .eslintrc.json: Extend next/core-web-vitals, add prettier plugin
- .prettierrc: Single quotes, trailing commas, 100 char width
- src/app/globals.css: @tailwind base/components/utilities; CSS variables for shadcn/ui theming (--background, --foreground, --primary, --secondary, --muted, --accent, --destructive, --border, --input, --ring, --radius)
- src/app/layout.tsx: Import globals.css, Inter font, ThemeProvider wrapper (to be created in Plan 02)
- src/app/page.tsx: Simple landing page with "Contributor Badge Program" heading and sign-in button placeholder
</action>
  <verify>
npm run build 2>&1 | head -20
</verify>
  <done>
Next.js project builds successfully, TypeScript compiles without errors, dev server starts on localhost:3000
</done>
</task>

<task type="auto">
  <name>task 2: Configure shadcn/ui with dark theme and install base components</name>
  <files>components.json, tailwind.config.ts, src/app/globals.css, src/lib/utils.ts, src/components/ui/button.tsx, src/components/ui/card.tsx, src/components/ui/progress.tsx, src/components/ui/label.tsx</files>
  <action>
Initialize shadcn/ui with `npx shadcn-ui@latest init` using defaults (TypeScript, default style, slate base color, CSS variables). Then install base components needed for dashboard: button, card, progress, label, badge, separator, scroll-area, avatar, dropdown-menu, toast, tooltip.
- components.json: Verify style=default, rsc=true, tsx=true, tailwind.config=tailwind.config.ts, css=src/app/globals.css
- tailwind.config.ts: Add content paths for src/components/**, ensure darkMode: 'class'
- src/app/globals.css: Verify all shadcn/ui CSS variables present (--background through --radius)
- src/lib/utils.ts: cn() helper for className merging (from shadcn/ui)
- Install components: button, card, progress, label, badge, separator, scroll-area, avatar, dropdown-menu, toast, tooltip via `npx shadcn-ui@latest add <component>`
- Test: Import Button from '@/components/ui/button' in page.tsx and render one
</action>
  <verify>
npm run build 2>&1 | grep -E "(button|card|progress)" && ls src/components/ui/
</verify>
  <done>
shadcn/ui configured, 11 base components installed and importable, build passes
</done>
</task>

<task type="auto">
  <name>task 3: Create theme provider and apply dark mode as default</name>
  <files>src/components/theme-provider.tsx, src/app/layout.tsx, src/app/globals.css</files>
  <action>
Create ThemeProvider using next-themes for client-side theme management with dark mode as default (no flash).
- Install next-themes: `npm install next-themes`
- src/components/theme-provider.tsx: Create ThemeProvider wrapper with attribute="class", defaultTheme="dark", enableSystem=false, disableTransitionOnChange=true
- src/app/layout.tsx: Wrap children with <ThemeProvider> (add 'use client' directive to theme-provider.tsx)
- src/app/globals.css: Ensure :root and .dark selectors have all CSS variables; body uses bg-background text-foreground
- Test: Verify page renders with dark background (slate-950) without flash on load
</action>
  <verify>
npm run build 2>&1 | tail -5 && curl -s http://localhost:3000 | grep -c "slate-950\|bg-background"
</verify>
  <done>
ThemeProvider works, dark mode is default, no flash on page load, build passes
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Build | npm packages from registry (supply chain) |
| Client → Browser | User-controlled input in future phases |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | npm supply chain | mitigate | Pin exact versions in package.json, use npm audit, enable 2FA on npm account |
| T-01-02 | Tampering | Build output | mitigate | Vercel builds from locked package-lock.json, immutable deployments |
| T-01-03 | Info Disclosure | Client bundle | accept | No secrets in client bundle (env vars server-only) |
| T-01-04 | DoS | Dev server | accept | Local only, not production |
</threat_model>

<verification>
- `npm run build` exits with code 0
- `npm run dev` starts server on port 3000
- Page renders with dark theme (slate-950 background)
- shadcn/ui Button component imports and renders
- TypeScript: `npx tsc --noEmit` passes
</verification>

<success_criteria>
- Next.js 14 App Router project scaffolded and building
- TypeScript strict mode enabled with path aliases
- Tailwind CSS configured with dark mode (class strategy)
- shadcn/ui initialized with 11 base components
- ThemeProvider with dark default, no flash
- All configs committed to git
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-foundation-01-SUMMARY.md`
</output>