---
phase: 03-admin-config
plan: 01
type: execute
wave: 1
depends_on: ["02-auth-profiles-02"]
files_modified:
  - src/app/admin/page.tsx
  - src/components/admin/settings-form.tsx
  - src/components/admin/repo-list.tsx
  - src/lib/admin.ts
  - src/app/api/admin/settings/route.ts
autonomous: true
requirements:
  - REQ-06
user_setup: []
must_haves:
  truths:
    - "Admin page shows form to configure GitHub org name"
    - "Admin page shows editable list of tracked repositories"
    - "Admin page shows webhook secret field (masked)"
    - "Form saves to maintainer_settings table (single row, id=1)"
    - "Only admins can access and modify settings"
    - "Validation: org name required, repos array, webhook secret min 20 chars"
  artifacts:
    - path: "src/app/admin/page.tsx"
      provides: "Admin settings page with form"
      exports: ["default"]
    - path: "src/components/admin/settings-form.tsx"
      provides: "Form component with org, repos, webhook secret fields"
      exports: ["SettingsForm"]
    - path: "src/components/admin/repo-list.tsx"
      provides: "Editable repo list with add/remove"
      exports: ["RepoList"]
    - path: "src/lib/admin.ts"
      provides: "Server actions for reading/writing maintainer_settings"
      exports: ["getSettings", "updateSettings"]
    - path: "src/app/api/admin/settings/route.ts"
      provides: "API route for settings CRUD (alternative to server actions)"
      exports: ["GET", "PUT"]
  key_links:
    - from: "src/components/admin/settings-form.tsx"
      to: "src/lib/admin.ts"
      via: "updateSettings()"
      pattern: "updateSettings"
    - from: "src/app/api/admin/settings/route.ts"
      to: "src/lib/supabase/server.ts"
      via: "supabase.from('maintainer_settings')"
      pattern: "maintainer_settings"
---

<objective>
Build admin configuration page for GitHub organization name, tracked repository list, and webhook secret. Use server actions for mutations with Supabase. Single-row maintainer_settings table (id=1).
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-auth-profiles/02-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>task 1: Create server actions for maintainer_settings CRUD</name>
  <files>src/lib/admin.ts, src/app/api/admin/settings/route.ts</files>
  <action>
Create server-side admin utilities and API route:

1. **src/lib/admin.ts** (Server Actions - 'use server'):
   - Import { createServerClient } from '@/lib/supabase/server'
   - Export async getSettings(): fetches row id=1 from maintainer_settings, returns { github_org_name, tracked_repos[], webhook_secret } or null
   - Export async updateSettings(data: { github_org_name: string; tracked_repos: string[]; webhook_secret: string }): upserts row id=1, returns updated row
   - Validation: org_name non-empty, tracked_repos is string[], webhook_secret min 20 chars
   - Revalidate path: '/admin'

2. **src/app/api/admin/settings/route.ts** (Alternative REST API):
   - GET: return settings (mask webhook_secret: show last 4 chars only)
   - PUT: validate input, call updateSettings, return updated (masked)
   - Both: check auth() and role === 'admin', else 403
   - Use createServerClient() for DB access

Note: Server Actions preferred for form mutations; API route for potential external use.
</action>
  <verify>
grep -c "getSettings\|updateSettings\|createServerClient\|maintainer_settings" src/lib/admin.ts src/app/api/admin/settings/route.ts
</verify>
  <done>
getSettings and updateSettings server actions created, API route with GET/PUT and admin auth check
</done>
</task>

<task type="auto">
  <name>task 2: Build SettingsForm component with org name, repo list, webhook secret</name>
  <files>src/components/admin/settings-form.tsx, src/components/admin/repo-list.tsx</files>
  <action>
Create form components using shadcn/ui (Form, Input, Button, Textarea, Alert):

1. **src/components/admin/repo-list.tsx** ('use client'):
   - Props: repos (string[]), onChange (repos: string[] => void)
   - Render list of repos with text input each + remove button (X)
   - "Add Repository" button adds empty input row
   - Validation: no duplicates, non-empty, format: owner/repo
   - Use shadcn/ui Input, Button (variant=ghost for remove)

2. **src/components/admin/settings-form.tsx** ('use client'):
   - Use React Hook Form with zod resolver
   - Schema: orgName (min 1), repos (array min 1), webhookSecret (min 20)
   - Fields:
     - GitHub Organization Name (Input, required)
     - Tracked Repositories (RepoList component)
     - Webhook Secret (Input type=password, show/hide toggle, helper: "Min 20 chars. Used to verify GitHub webhook signatures.")
   - Submit: calls updateSettings server action
   - Success: toast "Settings saved"
   - Error: toast with message
   - Load initial data via useEffect calling getSettings (or pass as prop from page)

3. Install: `npm install react-hook-form @hookform/resolvers zod`
</action>
  <verify>
ls src/components/admin/settings-form.tsx src/components/admin/repo-list.tsx && grep "react-hook-form\|zod\|RepoList\|updateSettings" src/components/admin/settings-form.tsx
</verify>
  <done>
SettingsForm with validation, RepoList with add/remove, form submits via server action, toasts on success/error
</done>
</task>

<task type="auto">
  <name>task 3: Wire admin page with settings form and initial data loading</name>
  <files>src/app/admin/page.tsx</files>
  <action>
Update admin page to fetch settings and render form:

1. **src/app/admin/page.tsx** (Server Component):
   - Import { auth } from '@/lib/auth', { getSettings } from '@/lib/admin'
   - Import SettingsForm from '@/components/admin/settings-form'
   - Export default async function AdminPage() {
       const session = await auth()
       if (!session?.user?.role || session.user.role !== 'admin') redirect('/dashboard')
       const settings = await getSettings()
       return (
         <div className="container mx-auto py-8 max-w-3xl">
           <h1 className="text-3xl font-bold mb-6">Admin Settings</h1>
           <SettingsForm initialSettings={settings} />
         </div>
       )
     }

2. **src/components/admin/settings-form.tsx** (update):
   - Accept initialSettings prop
   - Use useForm with defaultValues from initialSettings
   - On mount, if initialSettings provided, reset form

3. Add page metadata: title "Admin Settings | Contributor Badge Program"
</action>
  <verify>
grep "getSettings\|initialSettings\|SettingsForm" src/app/admin/page.tsx src/components/admin/settings-form.tsx
</verify>
  <done>
Admin page loads settings on server, passes to SettingsForm, form pre-fills and saves via server action
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin → Settings | Only admins can read/write |
| Webhook Secret → GitHub | Secret used to verify webhook signatures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Spoofing | Non-admin accessing admin page | mitigate | Middleware + server-side role check in page |
| T-03-02 | Tampering | Webhook secret exposure | mitigate | Mask in UI (show last 4), never log, HTTPS only |
| T-03-03 | Elevation | Contributor calling updateSettings | mitigate | Server action checks role via auth() |
| T-03-04 | Info Disclosure | Repo list enumeration | accept | Repos are public GitHub repositories |
</threat_model>

<verification>
- Sign in as admin -> /admin shows form with current settings
- Sign in as contributor -> /admin redirects to /dashboard
- Form validation: empty org name shows error, duplicate repos rejected
- Save updates maintainer_settings row id=1
- Webhook secret masked in UI (shows ••••abcd)
- Repo list adds/removes correctly
- Toast appears on successful save
</verification>

<success_criteria>
- Admin-only page at /admin with settings form
- GitHub org name field (required)
- Editable tracked repositories list (add/remove, owner/repo format)
- Webhook secret field (masked, min 20 chars)
- Server actions for get/update with validation
- Data persisted in maintainer_settings (single row, id=1)
- Role enforced at middleware, page, and server action levels
</success_criteria>

<output>
After completion, create `.planning/phases/03-admin-config/03-01-SUMMARY.md`
</output>