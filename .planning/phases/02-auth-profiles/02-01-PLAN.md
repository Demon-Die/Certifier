---
phase: 02-auth-profiles
plan: 01
type: execute
wave: 1
depends_on: ["01-foundation-03"]
files_modified:
  - src/app/auth/signin/page.tsx
  - src/app/auth/callback/page.tsx
  - src/components/auth/sign-in-button.tsx
  - src/components/auth/user-menu.tsx
  - src/app/layout.tsx
  - src/lib/auth.ts
autonomous: true
requirements:
  - REQ-04
user_setup: []
must_haves:
  truths:
    - "Unauthenticated users see 'Sign in with GitHub' button"
    - "Clicking sign-in redirects to GitHub OAuth"
    - "After authorization, user returns to app with active session"
    - "User menu shows GitHub avatar, username, role"
    - "Sign out clears session and returns to public page"
  artifacts:
    - path: "src/app/auth/signin/page.tsx"
      provides: "Sign-in page with GitHub button"
      exports: ["default"]
    - path: "src/app/auth/callback/page.tsx"
      provides: "OAuth callback handler (NextAuth handles automatically)"
      exports: ["default"]
    - path: "src/components/auth/sign-in-button.tsx"
      provides: "Reusable sign-in button component"
      exports: ["SignInButton"]
    - path: "src/components/auth/user-menu.tsx"
      provides: "Dropdown menu with user info and sign out"
      exports: ["UserMenu"]
    - path: "src/lib/auth.ts"
      provides: "signOut helper for client-side sign out"
      exports: ["signOut"]
  key_links:
    - from: "src/components/auth/sign-in-button.tsx"
      to: "/api/auth/signin/github"
      via: "NextAuth signIn()"
      pattern: "signIn\\(.*github"
    - from: "src/components/auth/user-menu.tsx"
      to: "src/lib/auth.ts"
      via: "signOut()"
      pattern: "signOut"
---

<objective>
Create sign-in page, sign-in button component, user menu with avatar/role display, and sign-out functionality. Wire up NextAuth flow end-to-end.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>task 1: Create sign-in page and reusable sign-in button component</name>
  <files>src/app/auth/signin/page.tsx, src/components/auth/sign-in-button.tsx</files>
  <action>
Create sign-in page and button component using shadcn/ui Button:

1. **src/components/auth/sign-in-button.tsx** ('use client'):
   - Import { signIn } from 'next-auth/react'
   - Export SignInButton component with variant="default", size="lg"
   - onClick: signIn('github', { callbackUrl: '/dashboard' })
   - Display: GitHub logo (SVG) + "Sign in with GitHub"
   - Disabled state while loading

2. **src/app/auth/signin/page.tsx** (Server Component):
   - Simple page with centered card
   - Heading: "Contributor Badge Program"
   - Subtext: "Sign in with GitHub to track your contributions and earn badges"
   - Render <SignInButton />
   - Link to / for home

3. Add GitHub SVG icon as inline component or use lucide-react Github icon
   - `npm install lucide-react`
</action>
  <verify>
ls src/app/auth/signin/page.tsx src/components/auth/sign-in-button.tsx && grep "signIn\|GitHub" src/components/auth/sign-in-button.tsx
</verify>
  <done>
Sign-in page renders with GitHub button, button triggers NextAuth GitHub OAuth flow
</done>
</task>

<task type="auto">
  <name>task 2: Create user menu component with avatar, username, role, and sign out</name>
  <files>src/components/auth/user-menu.tsx, src/lib/auth.ts</files>
  <action>
Create user menu dropdown using shadcn/ui DropdownMenu and Avatar:

1. **src/lib/auth.ts** (add client-side helper):
   - Export `signOut()` function using `signOut` from 'next-auth/react'
   - Redirect to '/' after sign out

2. **src/components/auth/user-menu.tsx** ('use client'):
   - Import { useSession } from 'next-auth/react'
   - Import { signOut } from '@/lib/auth'
   - Use DropdownMenu with Avatar trigger (src=session.user.image, alt=name)
   - Dropdown content:
     - DropdownMenuLabel: session.user.githubUsername
     - DropdownMenuSeparator
     - DropdownMenuItem: Role badge (contributor/maintainer/admin) - use Badge component with variant based on role
     - DropdownMenuSeparator
     - DropdownMenuItem onClick={signOut}: "Sign out" with LogOut icon
   - Handle loading state (show skeleton)

3. Role badge variants:
   - contributor: variant="secondary"
   - maintainer: variant="default"
   - admin: variant="destructive"
</action>
  <verify>
ls src/components/auth/user-menu.tsx && grep "useSession\|DropdownMenu\|Avatar\|signOut" src/components/auth/user-menu.tsx
</verify>
  <done>
UserMenu component shows avatar, username, role badge, and sign out; signOut clears session and redirects to home
</done>
</task>

<task type="auto">
  <name>task 3: Integrate user menu into layout and add auth callback page</name>
  <files>src/app/layout.tsx, src/app/auth/callback/page.tsx</files>
  <action>
Wire up authentication UI in root layout:

1. **src/app/layout.tsx** (update):
   - Import { Providers } from './providers' (already exists)
   - Import UserMenu from '@/components/auth/user-menu'
   - In header/nav area (create simple header if not exists):
     - Use { useSession } from 'next-auth/react' - but layout is Server Component
     - Alternative: Create Client Component wrapper for header with UserMenu
     - Create src/components/layout/header.tsx ('use client') with UserMenu
     - Import Header in layout.tsx

2. **src/app/auth/callback/page.tsx**:
   - NextAuth v4 handles callback automatically at /api/auth/callback/github
   - This page can be minimal or redirect to dashboard
   - Export default function Callback() { return <redirect to /dashboard /> } or use useEffect to redirect

3. Ensure layout.tsx has proper structure:
   - <html lang="en" className={theme}> (for dark mode)
   - <body className="bg-background text-foreground">
   - <Providers><Header /><main>{children}</main></Providers>
</action>
  <verify>
grep "Header\|UserMenu\|Providers" src/app/layout.tsx && ls src/components/layout/header.tsx
</verify>
  <done>
Header with UserMenu integrated in layout, auth callback redirects to dashboard, sign-in/sign-out flow works end-to-end
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → NextAuth | signIn/signOut calls |
| Session Cookie → Client | HttpOnly, Secure, SameSite=Lax |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | CSRF on sign-out | mitigate | NextAuth uses POST for signOut with CSRF token |
| T-02-02 | Info Disclosure | Username in URL | accept | GitHub username is public |
| T-02-03 | Tampering | Role badge spoofing | mitigate | Role from session (server-verified JWT), not client state |
</threat_model>

<verification>
- Visit /auth/signin -> shows GitHub button
- Click button -> redirects to GitHub OAuth
- Authorize -> returns to /dashboard with session
- User menu shows avatar, username, role badge
- Click sign out -> redirects to /, session cleared
- Refresh /dashboard -> redirects to sign-in (middleware)
</verification>

<success_criteria>
- Sign-in page with branded GitHub button
- Reusable SignInButton component
- UserMenu with avatar, username, role badge, sign out
- Header integrated in root layout
- Complete OAuth flow: sign in -> GitHub -> callback -> dashboard
- Sign out clears session and redirects home
- Middleware protects dashboard route
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-profiles/02-01-SUMMARY.md`
</output>